//~ Stateful and Stateless
// stateless widget - they don't have state and UI does not change, and it is immutable.
// stateful widget - it is mutable and it change UI dynamically when state change.

//~ Future and Stream - 
//Future is represent the asynchronous value (await)
// Stream store multiple asynchronous values over time (await for)
